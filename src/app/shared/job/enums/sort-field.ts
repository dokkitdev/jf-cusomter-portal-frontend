export enum JobSortField {
  JOB_ID = 'simpro_job_id',
  NAME = 'name',
  CUSTOMER_NAME = 'simpro_customer.name',
  SITE_NAME = 'simpro_site.name',
  PRIORITY = 'priority',
  COST_CENTER = 'cost_center_name',
  STAGE = 'stage',
  STATUS = 'job_status',
  REQUESTED = 'requested',
  APPOINTMENT = 'recent_schedule.date'
}
